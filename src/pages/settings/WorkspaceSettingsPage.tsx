import Box from "@mui/material/Box";
import Typography from "@mui/material/Typography";

export default function WorkspaceSettingsPage() {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h1" sx={{ mb: 1 }}>Workspace Settings</Typography>
      <Typography color="text.secondary">Workspace members, teams, billing, and security. Coming soon.</Typography>
    </Box>
  );
}
